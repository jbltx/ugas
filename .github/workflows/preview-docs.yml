name: Preview docs on pull request

on:
  pull_request:
  push:
    branches:
      - develop

concurrency:
  group: "preview-docs-${{ github.event.pull_request.number || github.ref_name }}"
  cancel-in-progress: true

jobs:
  preview-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      deployments: write
    environment:
      name: pull-request
      url: ${{ steps.deploy.outputs.pages-deployment-alias-url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Compute preview metadata
        id: ver
        run: |
          SHA="${{ github.event.pull_request.head.sha || github.sha }}"
          echo "version=dev-${SHA:0:7}" >> "$GITHUB_OUTPUT"
          PR_NUM="${{ github.event.pull_request.number }}"
          if [ -n "$PR_NUM" ]; then
            echo "branch=pr-${PR_NUM}" >> "$GITHUB_OUTPUT"
          else
            echo "branch=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve version placeholders
        run: |
          find schemas genres conformance -type f \( -name '*.json' -o -name '*.yaml' -o -name '*.md' \) \
            -exec sed -i "s/%%UGAS_VERSION%%/${{ steps.ver.outputs.version }}/g" {} +

      - name: Install Asciidoctor and Pandoc
        run: |
          sudo gem install asciidoctor
          sudo apt-get update -qq && sudo apt-get install -y -qq pandoc

      - name: Build SPEC.adoc with AsciiDoctor
        run: |
          mkdir -p dist
          asciidoctor \
            -a revnumber="${{ steps.ver.outputs.version }}" \
            -a ugas-version="${{ steps.ver.outputs.version }}" \
            -a stem=latexmath \
            SPEC.adoc \
            -o dist/index.html

      - name: Build genre packs with AsciiDoctor
        run: |
          for spec in genres/*/spec.adoc; do
            [ -f "$spec" ] || continue
            genre="$(basename "$(dirname "$spec")")"
            mkdir -p "dist/genres/${genre}"
            asciidoctor \
              -a revnumber="${{ steps.ver.outputs.version }}" \
              -a ugas-version="${{ steps.ver.outputs.version }}" \
              -a stem=latexmath \
              "$spec" \
              -o "dist/genres/${genre}/index.html"
          done

      - name: Build genre taxonomy with AsciiDoctor
        run: |
          if [ -f genres/taxonomy.adoc ]; then
            mkdir -p dist/genres
            asciidoctor \
              -a revnumber="${{ steps.ver.outputs.version }}" \
              -a ugas-version="${{ steps.ver.outputs.version }}" \
              -a stem=latexmath \
              genres/taxonomy.adoc \
              -o dist/genres/taxonomy.html
          fi

      - name: Build genre packs index with AsciiDoctor
        run: |
          if [ -f genres/index.adoc ]; then
            mkdir -p dist/genres
            asciidoctor \
              -a revnumber="${{ steps.ver.outputs.version }}" \
              -a ugas-version="${{ steps.ver.outputs.version }}" \
              -a stem=latexmath \
              genres/index.adoc \
              -o dist/genres/index.html
          fi

      - name: Generate LLM files
        run: |
          bash scripts/generate-llm-files.sh . dist "${{ steps.ver.outputs.version }}"

      - name: Set up Python for artifacts
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install artifact deps
        run: python3 -m pip install --quiet jsonschema PyYAML

      - name: Generate + verify consumer artifacts
        run: |
          # dist/ is the preview's version tree; give it the source files the
          # manifest and generators list (the genre HTML is already in dist/genres).
          cp -r schemas dist/schemas
          cp SPEC.adoc dist/SPEC.adoc
          cp -r genres/. dist/genres/
          [ -d conformance ] && cp -r conformance dist/conformance
          bash scripts/build-artifacts.sh dist "${{ steps.ver.outputs.version }}" dist/llms.txt
          python3 scripts/check_artifacts.py dist dist/llms.txt

      - name: Deploy to Cloudflare Pages (preview)
        id: deploy
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # Pinned deliberately. Unpinned, the action installs whatever `wrangler@4`
          # currently resolves to, so a broken upstream publish breaks every PR here
          # within seconds of hitting npm — which is exactly what happened on
          # 2026-08-11: wrangler 4.121.0 shipped exact-pinning
          # `miniflare@5.20260804.1-alpha`, a version that was never published, so
          # `npm i wrangler@4` failed with ETARGET. Bump this after checking the
          # release resolves and is provenance-signed.
          wranglerVersion: "4.120.1"
          command: pages deploy dist --project-name=ugas --branch=${{ steps.ver.outputs.branch }}

      - name: Post preview URL as PR comment
        uses: actions/github-script@v9
        if: github.event_name == 'pull_request'
        with:
          script: |
            const url = '${{ steps.deploy.outputs.deployment-url }}';
            if (!url) {
              core.warning('No deployment URL found; skipping PR comment.');
              return;
            }
            const sha  = '${{ github.event.pull_request.head.sha }}'.slice(0, 7);
            const body = `## 📄 Spec preview\n\n` +
              `Deployed from commit \`${sha}\`\n\n` +
              `**→ ${url}**`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              issue_number: context.issue.number,
            });

            // Match by the GitHub Actions bot user ID (41898282) — stable across renames
            const existing = comments.find(c =>
              c.user.type === 'Bot' &&
              c.user.id === 41898282 &&
              c.body.startsWith('## 📄 Spec preview')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo:  context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo:  context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
